if(WIN32)
    set(platform_sources namedpipelink.cpp)
endif(WIN32)
if(UNIX)
    set(platform_sources fifolink.cpp)
endif(UNIX)
add_library(parachutedev STATIC log.cpp misc.cpp hexdump.cpp link.cpp "${platform_sources}" linkfactory.cpp platformdetection.h)