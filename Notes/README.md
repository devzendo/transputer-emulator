Documentation, and code compiled on the older gcc-t800/ttools system (that I
couldn't get rebuilt on modern 64-bit OSs).

Any *.asta files here are UML models developed with Astah Community Edition
which no longer exists as a product - there are other editions, but they
require a very expensive subscription. Any useful diagrams are exported as
.png files.
